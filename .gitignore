# Go
server/bin/
server/tmp/
*.exe
*.test
*.out
mockoidc/mockoidc

# Node / React
client/node_modules/
client/dist/
client/.env
client/.env.local

# Playwright
client/test-results/
client/playwright-report/
client/blob-report/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code local overrides (personal, may contain locally-approved
# permission entries with embedded secrets — never intended to be shared)
.claude/settings.local.json

# Environment
.env
.env.local
.github-token.local

# Server secrets
server/config.json

# EC2 deployment env (secrets)
maintenance/ec2/.env
maintenance/ec2/config/config.json

# ECR build env
maintenance/ecr/.env

maintenance/alb/create-alb.sh

# Terraform (commit .terraform.lock.hcl for reproducible provider versions)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log

