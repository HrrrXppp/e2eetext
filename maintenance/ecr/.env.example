# AWS account and region for ECR.
AWS_ACCOUNT_ID=123456789012
AWS_REGION=us-east-1

# Image tag pushed to ECR (e.g. latest, v1.0.0, git sha).
IMAGE_TAG=latest

# Target platform for EC2 (use linux/amd64 when building on Apple Silicon).
DOCKER_PLATFORM=linux/amd64
